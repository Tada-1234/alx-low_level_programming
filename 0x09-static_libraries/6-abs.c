#include "main.h"
#include <stdlib.h>
/**
 * _abs - display absolute of the input
 * @x: holds the integer we want to find abs of
 *
 * Return: returns abs(x)
 */
int _abs(int x)
{
	return (abs(x));
}
