#include "main.h"
#include <ctype.h>
/**
 * print_numbers - prints numbers from 0 to 9
 *
 * Return: none
 */
void print_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
