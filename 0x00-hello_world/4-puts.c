#include <stdio.h>
/**
 *main - Entry point
 *
 *a program the prints the contents of a char onto the screen then returns 0
 *cisfun: holds the char we want to display
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	char cisfun[] = "\"Programming is like building a multilingual puzzle";

	puts(cisfun);
	return (0);
}
