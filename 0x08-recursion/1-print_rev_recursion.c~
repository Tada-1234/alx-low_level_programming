/**
 * _print_rev_recursion - writes the character c to stdout in reverse
 * @s: The character to print
 *
 * Return: Nothing
 */
void _print_rev_recursion(char *s)
{
	if (*s)
	{
		_print_rev_recursion(s + 1);
		_putchar(*s);
	}
}
