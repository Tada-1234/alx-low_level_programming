#include "main.h"
/**
 * factorial - finds factorial of n
 * @n: The int we want the factorial of
 *
 * Return: factorial of n (int)
 */
int factorial(int n)
{
	if (n == 0)
	{
		return (1);
	}
	return (n * factorial(n - 1));
}
