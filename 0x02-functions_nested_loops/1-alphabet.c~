#include <stdio.h>
#include "main.h"

int main(void)
{
	print_alphabet();
	return (0);
}

void print_alphabet(void)
{
	char alpha_bet;
	int i;
	alpha_bet = 'a';
	for (i = 0; i < 26; i++)
	{
		_putchar(alpha_bet);
		alpha_bet++;
	}
	_putchar('\n');
}
