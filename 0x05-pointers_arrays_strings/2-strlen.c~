#include "main.h"
#include <string.h>
/**
 * _strlen - prints the length of a string
 * @s: holds the pointer for a string
 *
 * Return: length of string
 */
int _strlen(char *s)
{
	return (strlen(*s));
}
